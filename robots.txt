# robots.txt — Ursa Coffee Strategic Dossier
# Static-exported Next.js app on GitHub Pages.
# All hash routes share the single root URL, so we allow full crawl
# and let the sitemap enumerate the indexable HTML pages.

User-agent: *
Allow: /

# Block nothing — the dossier HTML files and assets are all public.

# Sitemap (absolute URL — owner: update if a custom domain is configured)
Sitemap: https://ursacoffeeperu.github.io/AIMarket-Design-Consulting-Reports/sitemap.xml
